{
  "claims": [
    {
      "claim_id": "contract_elements_are_a_closed_vocabulary",
      "description": "Every required/forbidden element policy.py can emit is a known member of the guard's vocabulary, and each one either has a matcher or is explicitly delegated to another rule family.",
      "why_it_matters": "check_contract() used to skip unknown elements silently. 11 of 20 declared elements had no matcher and were never checked at all \u2014 including 'an offer to request the policyholder's consent', a live R8 requirement.",
      "status": "ok",
      "failures": []
    },
    {
      "claim_id": "end_reasons_have_exactly_one_home",
      "description": "Every way a session can end is declared once, in types.END_REASONS, and every consumer resolves against it rather than keeping a private copy.",
      "why_it_matters": "Five overlapping representations of 'why did this end' (phase, escalation_reason, terminal_reason, route, disposition code) were kept in sync by hand. Needing a consistency test between internal representations is the smell.",
      "status": "ok",
      "failures": []
    },
    {
      "claim_id": "every_directive_declares_its_backing",
      "description": "Each directive states which mechanism enforces it: structural (the data or tool is absent), deterministic (code decides), guard (an output rule blocks it), or advisory (tone only \u2014 nothing enforces it, and that is admitted).",
      "why_it_matters": "SEND_NOW never fired once for an entire build. The email went out anyway because the model volunteered the tool call. A directive feels like enforcement and is not; making the backing explicit is what stops that feeling being load-bearing.",
      "status": "ok",
      "failures": []
    },
    {
      "claim_id": "client_events_are_a_closed_vocabulary",
      "description": "Every event name the frontend emits is known to the metrics sink.",
      "why_it_matters": "record_event() drops unknown names silently, so a renamed event becomes a metric that quietly reads zero forever \u2014 indistinguishable from 'this never happens'.",
      "status": "ok",
      "failures": []
    },
    {
      "claim_id": "ui_phase_vocabulary_matches_the_engine",
      "description": "Phase names hard-coded in the frontend correspond to real engine phases.",
      "why_it_matters": "The UI keys copy and layout off phase strings. A renamed phase leaves the interface silently rendering nothing for it.",
      "status": "ok",
      "failures": []
    }
  ],
  "summary": {
    "claims_total": 5,
    "claims_ok": 5,
    "drifted": [],
    "failures": 0,
    "coherent": true
  }
}